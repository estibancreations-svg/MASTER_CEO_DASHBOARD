# Browser-safe configuration only.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key

# Keep true during buildout. Set exactly false only after the authenticated
# organization, role, denied-session, expired-session and sign-out tests pass.
VITE_BUILDER_MODE=true
