import{FormEvent,ReactNode,useEffect,useState}from'react';import type{Session}from'@supabase/supabase-js';import{isSupabaseConfigured,supabase}from'../lib/supabase';
export default function AuthGate({children}:{children:ReactNode}){const[session,setSession]=useState<Session|null>(null),[ready,setReady]=useState(!isSupabaseConfigured),[email,setEmail]=useState(''),[sent,setSent]=useState(false),[error,setError]=useState('');useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>{setSession(data.session);setReady(true)});const{data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);const login=async(e:FormEvent)=>{e.preventDefault();if(!supabase)return;setError('');const{error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:location.origin}});if(error)setError(error.message);else setSent(true)};if(!ready)return <div className="auth-screen"><div className="auth-card"><div className="brand-mark">EC</div><h1>Verifying executive session</h1></div></div>;if(isSupabaseConfigured&&!session)return <div className="auth-screen"><form className="auth-card" onSubmit={login}><div className="brand-mark">EC</div><span className="eyebrow">SYS-CEO-001 · ZERO-TRUST ENTRY</span><h1>CEO Command Center</h1><p>Use an authorized executive account. Access is restricted by server-side role policy.</p><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Executive email"/><button type="submit">Send secure sign-in link</button>{sent&&<small>Check your email for the secure link.</small>}{error&&<small className="auth-error">{error}</small>}</form></div>;return <>{children}</>}
